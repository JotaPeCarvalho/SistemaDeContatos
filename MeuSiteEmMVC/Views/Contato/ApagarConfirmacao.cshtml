@model ContatoModel
@{
    ViewData["Title"] = "Confirmação apagar o Contato";
}

<div class="text-center mb-3">
    <h3>Realmente deseja apagar o contato (@Model.Nome)?</h3>
</div>
<a class="btn btn-primary d-block mb-2" role="button" asp-controller="Contato" asp-action="Index">Cancelar Exclusão</a>
<a class="btn btn-danger d-block" role="button" asp-route-id="@Model.Id" asp-controller="Contato" asp-action="Apagar">Confirmar Exclusão</a>

